# Tested floors. To reproduce an exact result: pip freeze > requirements-lock.txt
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
pandas>=2.0        # only to lay out the comparison tables in the notebook

# Optional. test_lab.py also runs standalone: python test_lab.py
pytest>=7.0
